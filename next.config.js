
const nextConfig = {
    output: 'export',
    distDir: 'docs',
    basePath: '/MindMapping',
    trailingSlash: true,
    images: {
        unoptimized: true,
    },
};

module.exports = nextConfig;